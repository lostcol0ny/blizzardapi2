diff --git b/blizzardapi2/wow/wow_game_data_api.py a/blizzardapi2/wow/wow_game_data_api.py
index bec0194..d1a6102 100644
--- b/blizzardapi2/wow/wow_game_data_api.py
+++ a/blizzardapi2/wow/wow_game_data_api.py
@@ -4,7 +4,6 @@ from typing import Any
 
-from ..api import LocaleApi
-from ..types import OptionalLocale, OptionalRegion
+from ..api import BaseApi
 
 
-class WowGameDataApi(LocaleApi):
+class WowGameDataApi(BaseApi):
     """All Wow Game Data API methods.
@@ -14,74 +13,13 @@ class WowGameDataApi(LocaleApi):
         client_secret (str): A string client secret supplied by Blizzard.
-        region (str, optional): A default region to use for requests.
-        locale (str, optional): A default locale to use for requests.
     """
 
-    def _get_resource(
-        self,
-        resource: str,
-        namespace: str,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        **query_params: Any,
-    ) -> dict[str, Any]:
-        """Internal function to get resources from the World of Warcraft Game Data API
-
-        Args:
-            resource (str): the requested resource
-            namespace (str): the namespace in which the requested resource lives
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            **query_params (dict, optional): additional arguments to to the resource request to support search requests.
-
-        Returns:
-            dict[str, Any]: the JSON response transformed to a dict structure.
-        """
-        query_params.update({"namespace": f"{namespace}-{region or self.region}"})
-        return super().get_resource(resource, region, locale, query_params)
-
-    def _get_static_resource(
-        self,
-        resource: str,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
-        **search_params: Any,
-    ) -> dict[str, Any]:
-        """Internal function to get resources from the World of Warcraft Game Data API that use a static namespace
-
-        Args:
-            resource (str): the requested resource
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            is_classic (bool, optional): a boolean flag indicating whether to use a Classic WOW namespace. Defaults to False.
-            **search_params (optional): additional arguments to to the resource request to support search requests.
-
-        Returns:
-            dict[str, Any]: the JSON response transformed to a dict structure.
+    def __init__(self, client_id: str, client_secret: str) -> None:
         """
-        namespace = "static-classic" if is_classic else "static"
-        return self._get_resource(resource, namespace, region, locale, **search_params)
-
-    def _get_dynamic_resource(
-        self,
-        resource: str,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
-        **search_params: Any,
-    ) -> dict[str, Any]:
-        """Internal function to get resources from the World of Warcraft Game Data API that use a dynamic namespace
+        Initialize the WowGameDataApi class.
 
         Args:
-            resource (str): the requested resource
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            is_classic (bool, optional): a boolean flag indicating whether to use a Classic WOW namespace. Defaults to False.
-            **search_params (optional): additional arguments to to the resource request to support search requests.
-
-        Returns:
-            dict[str, Any]: the JSON response transformed to a dict structure.
+            client_id (str): The client ID supplied by Blizzard.
+            client_secret (str): The client secret supplied by Blizzard.
         """
-        namespace = "dynamic-classic" if is_classic else "dynamic"
-        return self._get_resource(resource, namespace, region, locale, **search_params)
+        super().__init__(client_id, client_secret)
 
@@ -89,5 +27,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_achievements_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_achievements_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -96,4 +32,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -103,10 +39,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/achievement/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_achievement(
-        self,
-        achievement_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, achievement_id: int
     ) -> dict[str, Any]:
@@ -116,5 +49,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             achievement_id (int): The ID of the achievement to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -124,6 +57,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/achievement/{achievement_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_achievement_categories_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
+        self, region: str, locale: str
     ) -> dict[str, Any]:
@@ -133,4 +67,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -140,10 +74,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/achievement-category/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_achievement_category(
-        self,
-        achievement_category_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, achievement_category_id: int
     ) -> dict[str, Any]:
@@ -153,5 +84,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             achievement_category_id (int): The ID of the achievement category to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -161,10 +92,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/achievement-category/{achievement_category_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_achievement_media(
-        self,
-        achievement_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, achievement_id: int
     ) -> dict[str, Any]:
@@ -174,5 +102,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             achievement_id (int): The ID of the achievement to retrieve media for.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -182,3 +110,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/achievement/{achievement_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -187,7 +116,3 @@ class WowGameDataApi(LocaleApi):
     def get_auction_house_index(
-        self,
-        connected_realm_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, connected_realm_id: int
     ) -> dict[str, Any]:
@@ -199,5 +124,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             connected_realm_id (int): The ID of the connected realm.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -207,11 +132,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/connected-realm/{connected_realm_id}/auctions/index"
-        return self._get_dynamic_resource(resource, region, locale, is_classic=True)
+        query_params = {"namespace": f"dynamic-classic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_auctions_for_auction_house(
-        self,
-        connected_realm_id: int,
-        auction_house_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, connected_realm_id: int, auction_house_id: int
     ) -> dict[str, Any]:
@@ -223,6 +144,6 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             connected_realm_id (int): The ID of the connected realm.
             auction_house_id (int): The ID of the auction house.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -232,10 +153,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/connected-realm/{connected_realm_id}/auctions/{auction_house_id}"
-        return self._get_dynamic_resource(resource, region, locale, is_classic=True)
+        query_params = {"namespace": f"dynamic-classic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_auctions(
-        self,
-        connected_realm_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, connected_realm_id: int
     ) -> dict[str, Any]:
@@ -245,5 +163,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             connected_realm_id (int): The ID of the connected realm.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -253,7 +171,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/connected-realm/{connected_realm_id}/auctions"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_commodities(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_commodities(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -267,4 +184,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -274,3 +191,4 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/auctions/commodities"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -278,5 +196,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_azerite_essences_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_azerite_essences_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -285,4 +201,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -292,10 +208,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/azerite-essence/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_azerite_essence(
-        self,
-        azerite_essence_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, azerite_essence_id: int
     ) -> dict[str, Any]:
@@ -305,5 +218,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             azerite_essence_id (int): The ID of the Azerite essence to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -313,10 +226,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/azerite-essence/{azerite_essence_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_azerite_essence_media(
-        self,
-        azerite_essence_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, azerite_essence_id: int
     ) -> dict[str, Any]:
@@ -326,5 +236,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             azerite_essence_id (int): The ID of the Azerite essence to retrieve media for.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -334,3 +244,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/azerite-essence/{azerite_essence_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -339,7 +250,3 @@ class WowGameDataApi(LocaleApi):
     def get_connected_realms_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -349,4 +256,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -357,3 +264,5 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/connected-realm/index"
-        return self._get_dynamic_resource(resource, region, locale, is_classic)
+        namespace = f"dynamic-classic-{region}" if is_classic else f"dynamic-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -361,6 +270,5 @@ class WowGameDataApi(LocaleApi):
         self,
+        region: str,
+        locale: str,
         connected_realm_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
         is_classic: bool = False,
@@ -371,5 +279,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             connected_realm_id (int): The ID of the connected realm to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -380,3 +288,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/connected-realm/{connected_realm_id}"
-        return self._get_dynamic_resource(resource, region, locale, is_classic)
+        namespace = f"dynamic-classic-{region}" if is_classic else f"dynamic-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -384,5 +294,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_covenant_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_covenant_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -391,4 +299,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -398,10 +306,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/covenant/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_covenant(
-        self,
-        covenant_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, covenant_id: int
     ) -> dict[str, Any]:
@@ -411,5 +316,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             covenant_id (int): The ID of the covenant to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -419,10 +324,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/covenant/{covenant_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_covenant_media(
-        self,
-        covenant_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, covenant_id: int
     ) -> dict[str, Any]:
@@ -432,5 +334,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             covenant_id (int): The ID of the covenant to retrieve media for.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -440,7 +342,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/covenant/{covenant_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_soulbind_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_soulbind_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -449,4 +350,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -456,10 +357,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/covenant/soulbind/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_soulbind(
-        self,
-        soulbind_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, soulbind_id: int
     ) -> dict[str, Any]:
@@ -469,5 +367,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             soulbind_id (int): The ID of the soulbind to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -477,7 +375,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/covenant/soulbind/{soulbind_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_conduit_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_conduit_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -486,4 +383,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -493,11 +390,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/covenant/conduit/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_conduit(
-        self,
-        conduit_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_conduit(self, region: str, locale: str, conduit_id: int) -> dict[str, Any]:
         """
@@ -506,5 +398,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             conduit_id (int): The ID of the conduit to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -514,3 +406,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/covenant/conduit/{conduit_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -519,7 +412,3 @@ class WowGameDataApi(LocaleApi):
     def get_creature_families_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -529,4 +418,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -537,3 +426,5 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/creature-family/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -541,6 +432,5 @@ class WowGameDataApi(LocaleApi):
         self,
+        region: str,
+        locale: str,
         creature_family_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
         is_classic: bool = False,
@@ -551,5 +441,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             creature_family_id (int): The ID of the creature family to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -560,10 +450,8 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/creature-family/{creature_family_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_creature_types_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -573,4 +461,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -581,11 +469,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/creature-type/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_creature_type(
-        self,
-        creature_type_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, creature_type_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -595,5 +480,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             creature_type_id (int): The ID of the creature type to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -604,11 +489,8 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/creature-type/{creature_type_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_creature(
-        self,
-        creature_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, creature_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -618,5 +500,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             creature_id (int): The ID of the creature to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -627,3 +509,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/creature/{creature_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -631,6 +515,5 @@ class WowGameDataApi(LocaleApi):
         self,
+        region: str,
+        locale: str,
         creature_display_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
         is_classic: bool = False,
@@ -641,5 +524,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             creature_display_id (int): The ID of the creature display to retrieve media for.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -650,3 +533,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/creature-display/{creature_display_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -654,6 +539,5 @@ class WowGameDataApi(LocaleApi):
         self,
+        region: str,
+        locale: str,
         creature_family_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
         is_classic: bool = False,
@@ -664,5 +548,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             creature_family_id (int): The ID of the creature family to retrieve media for.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -673,3 +557,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/creature-family/{creature_family_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -678,7 +564,3 @@ class WowGameDataApi(LocaleApi):
     def get_guild_crest_components_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -688,4 +570,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -696,11 +578,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/guild-crest/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_guild_crest_border_media(
-        self,
-        border_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, border_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -710,5 +589,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             border_id (int): The ID of the guild crest border to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -719,11 +598,8 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/guild-crest/border/{border_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_guild_crest_emblem_media(
-        self,
-        emblem_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, emblem_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -733,5 +609,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             emblem_id (int): The ID of the guild crest emblem to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -742,3 +618,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/guild-crest/emblem/{emblem_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -746,5 +624,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_decor_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_decor_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -753,4 +629,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -760,10 +636,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/decor/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_decor(
-        self,
-        decor_id: int,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_decor(self, region: str, locale: str, decor_id: int) -> dict[str, Any]:
         """
@@ -772,5 +644,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             decor_id (int): The ID of the decor to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -780,10 +652,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/decor/{decor_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def search_decor(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        **search_params: Any,
+        self, region: str, locale: str, **query_params: Any
     ) -> dict[str, Any]:
@@ -793,5 +662,5 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            **search_params (Any): Additional search parameters (e.g., name.en_US, orderby, _page).
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
+            **query_params (Any): Additional search parameters (e.g., name.en_US, orderby, _page).
 
@@ -801,3 +670,4 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/search/decor"
-        return self._get_static_resource(resource, region, locale, **search_params)
+        query_params.update({"namespace": f"static-{region}", "locale": locale})
+        return super().get_resource(resource, region, query_params)
 
@@ -805,5 +675,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_fixture_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_fixture_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -812,4 +680,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -819,11 +687,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/fixture/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_fixture(
-        self,
-        fixture_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_fixture(self, region: str, locale: str, fixture_id: int) -> dict[str, Any]:
         """
@@ -832,5 +695,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             fixture_id (int): The ID of the fixture to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -840,10 +703,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/fixture/{fixture_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def search_fixture(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        **search_params: Any,
+        self, region: str, locale: str, **query_params: Any
     ) -> dict[str, Any]:
@@ -853,5 +713,5 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            **search_params (Any): Additional search parameters (e.g., name.en_US, orderby, _page).
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
+            **query_params (Any): Additional search parameters (e.g., name.en_US, orderby, _page).
 
@@ -861,3 +721,4 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/search/fixture"
-        return self._get_static_resource(resource, region, locale, **search_params)
+        query_params.update({"namespace": f"static-{region}", "locale": locale})
+        return super().get_resource(resource, region, query_params)
 
@@ -865,5 +726,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_fixture_hook_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_fixture_hook_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -872,4 +731,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -879,10 +738,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/fixture-hook/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_fixture_hook(
-        self,
-        fixture_hook_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, fixture_hook_id: int
     ) -> dict[str, Any]:
@@ -892,5 +748,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             fixture_hook_id (int): The ID of the fixture hook to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -900,10 +756,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/fixture-hook/{fixture_hook_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def search_fixture_hook(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        **search_params: Any,
+        self, region: str, locale: str, **query_params: Any
     ) -> dict[str, Any]:
@@ -913,5 +766,5 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            **search_params (Any): Additional search parameters (e.g., name.en_US, orderby, _page).
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
+            **query_params (Any): Additional search parameters (e.g., name.en_US, orderby, _page).
 
@@ -921,3 +774,4 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/search/fixture-hook"
-        return self._get_static_resource(resource, region, locale, **search_params)
+        query_params.update({"namespace": f"static-{region}", "locale": locale})
+        return super().get_resource(resource, region, query_params)
 
@@ -925,5 +779,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_room_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_room_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -932,4 +784,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -939,11 +791,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/room/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_room(
-        self,
-        room_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_room(self, region: str, locale: str, room_id: int) -> dict[str, Any]:
         """
@@ -952,5 +799,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             room_id (int): The ID of the room to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -960,10 +807,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/room/{room_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def search_room(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        **search_params: Any,
+        self, region: str, locale: str, **query_params: Any
     ) -> dict[str, Any]:
@@ -973,5 +817,5 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            **search_params (Any): Additional search parameters (e.g., name.en_US, orderby, _page).
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
+            **query_params (Any): Additional search parameters (e.g., name.en_US, orderby, _page).
 
@@ -981,3 +825,4 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/search/room"
-        return self._get_static_resource(resource, region, locale, **search_params)
+        query_params.update({"namespace": f"static-{region}", "locale": locale})
+        return super().get_resource(resource, region, query_params)
 
@@ -985,5 +830,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_heirloom_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_heirloom_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -992,4 +835,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -999,10 +842,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/heirloom/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_heirloom(
-        self,
-        heirloom_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, heirloom_id: int
     ) -> dict[str, Any]:
@@ -1012,5 +852,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             heirloom_id (int): The ID of the heirloom to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1020,3 +860,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/heirloom/{heirloom_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1025,7 +866,3 @@ class WowGameDataApi(LocaleApi):
     def get_item_classes_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1035,4 +872,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1043,11 +880,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/item-class/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item_class(
-        self,
-        item_class_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, item_class_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1057,5 +891,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             item_class_id (int): The ID of the item class to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1066,10 +900,8 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/item-class/{item_class_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item_sets_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1079,4 +911,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1087,11 +919,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/item-set/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item_set(
-        self,
-        item_set_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, item_set_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1101,5 +930,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             item_set_id (int): The ID of the item set to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1110,3 +939,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/item-set/{item_set_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1114,7 +945,6 @@ class WowGameDataApi(LocaleApi):
         self,
+        region: str,
+        locale: str,
         item_class_id: int,
         item_subclass_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
         is_classic: bool = False,
@@ -1125,6 +955,6 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             item_class_id (int): The ID of the item class.
             item_subclass_id (int): The ID of the item subclass to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1137,11 +967,8 @@ class WowGameDataApi(LocaleApi):
         )
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item(
-        self,
-        item_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, item_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1151,5 +978,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             item_id (int): The ID of the item to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1160,11 +987,8 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/item/{item_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item_media(
-        self,
-        item_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, item_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1174,5 +998,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             item_id (int): The ID of the item to retrieve media for.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1183,3 +1007,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/item/{item_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1188,8 +1014,3 @@ class WowGameDataApi(LocaleApi):
     def get_item_appearance(
-        self,
-        appearance_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, appearance_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1199,5 +1020,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             appearance_id (int): The ID of the item appearance to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1208,10 +1029,8 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/item-appearance/{appearance_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item_appearance_sets_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1221,4 +1040,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1229,11 +1048,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/item-appearance/set/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item_appearance_set(
-        self,
-        appearance_set_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, appearance_set_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1243,5 +1059,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             appearance_set_id (int): The ID of the item appearance set to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1252,10 +1068,8 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/item-appearance/set/{appearance_set_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item_appearance_slot_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1265,4 +1079,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1273,11 +1087,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/item-appearance/slot/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_item_appearance_slot(
-        self,
-        slot_type: str,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, slot_type: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1287,5 +1098,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             slot_type (str): The type of slot to retrieve (e.g., "head", "chest").
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1296,3 +1107,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/item-appearance/slot/{slot_type}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1300,5 +1113,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_journal_expansions_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_journal_expansions_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -1307,4 +1118,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -1314,10 +1125,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/journal-expansion/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_journal_expansion(
-        self,
-        journal_expansion_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, journal_expansion_id: int
     ) -> dict[str, Any]:
@@ -1327,5 +1135,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             journal_expansion_id (int): The ID of the journal expansion to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1335,7 +1143,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/journal-expansion/{journal_expansion_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_journal_encounters_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_journal_encounters_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -1344,4 +1151,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -1351,10 +1158,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/journal-encounter/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_journal_encounter(
-        self,
-        journal_encounter_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, journal_encounter_id: int
     ) -> dict[str, Any]:
@@ -1364,5 +1168,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             journal_encounter_id (int): The ID of the journal encounter to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1372,7 +1176,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/journal-encounter/{journal_encounter_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_journal_instances_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_journal_instances_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -1381,4 +1184,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -1388,10 +1191,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/journal-instance/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_journal_instance(
-        self,
-        journal_instance_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, journal_instance_id: int
     ) -> dict[str, Any]:
@@ -1401,5 +1201,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             journal_instance_id (int): The ID of the journal instance to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1409,10 +1209,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/journal-instance/{journal_instance_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_journal_instance_media(
-        self,
-        journal_instance_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, journal_instance_id: int
     ) -> dict[str, Any]:
@@ -1422,5 +1219,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             journal_instance_id (int): The ID of the journal instance to retrieve media for.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1430,3 +1227,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/journal-instance/{journal_instance_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1434,5 +1232,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_modified_crafting_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_modified_crafting_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -1441,4 +1237,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -1448,6 +1244,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/modified-crafting/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_modified_crafting_category_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
+        self, region: str, locale: str
     ) -> dict[str, Any]:
@@ -1457,4 +1254,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -1464,10 +1261,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/modified-crafting/category/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_modified_crafting_category(
-        self,
-        category_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, category_id: int
     ) -> dict[str, Any]:
@@ -1477,5 +1271,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             category_id (int): The ID of the Modified Crafting category to retrieve.
-            region (Region, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (Locale, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1485,6 +1279,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/modified-crafting/category/{category_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_modified_crafting_reagent_slot_type_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
+        self, region: str, locale: str
     ) -> dict[str, Any]:
@@ -1494,4 +1289,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -1501,10 +1296,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/modified-crafting/reagent-slot-type/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_modified_crafting_reagent_slot_type(
-        self,
-        slot_type_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, slot_type_id: int
     ) -> dict[str, Any]:
@@ -1514,5 +1306,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             slot_type_id (int): The ID of the reagent slot type to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1522,3 +1314,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/modified-crafting/reagent-slot-type/{slot_type_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1526,5 +1319,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_mounts_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_mounts_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -1533,4 +1324,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -1540,11 +1331,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/mount/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_mount(
-        self,
-        mount_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_mount(self, region: str, locale: str, mount_id: int) -> dict[str, Any]:
         """
@@ -1553,5 +1339,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             mount_id (int): The ID of the mount to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1561,3 +1347,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/mount/{mount_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1566,3 +1353,3 @@ class WowGameDataApi(LocaleApi):
     def get_mythic_keystone_affixes_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
+        self, region: str, locale: str
     ) -> dict[str, Any]:
@@ -1572,4 +1359,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -1579,10 +1366,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/keystone-affix/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_mythic_keystone_affix(
-        self,
-        keystone_affix_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, keystone_affix_id: int
     ) -> dict[str, Any]:
@@ -1592,5 +1376,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             keystone_affix_id (int): The ID of the mythic keystone affix to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1600,10 +1384,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/keystone-affix/{keystone_affix_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_mythic_keystone_affix_media(
-        self,
-        keystone_affix_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, keystone_affix_id: int
     ) -> dict[str, Any]:
@@ -1613,5 +1394,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             keystone_affix_id (int): The ID of the mythic keystone affix to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1621,3 +1402,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/keystone-affix/{keystone_affix_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1626,3 +1408,3 @@ class WowGameDataApi(LocaleApi):
     def get_mythic_keystone_dungeons_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
+        self, region: str, locale: str
     ) -> dict[str, Any]:
@@ -1632,4 +1414,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -1639,10 +1421,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/mythic-keystone/dungeon/index"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_mythic_keystone_dungeon(
-        self,
-        dungeon_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, dungeon_id: int
     ) -> dict[str, Any]:
@@ -1652,5 +1431,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             dungeon_id (int): The ID of the dungeon to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1660,7 +1439,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/mythic-keystone/dungeon/{dungeon_id}"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_mythic_keystone_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_mythic_keystone_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -1669,4 +1447,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -1676,6 +1454,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/mythic-keystone/index"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_mythic_keystone_periods_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
+        self, region: str, locale: str
     ) -> dict[str, Any]:
@@ -1685,4 +1464,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -1692,10 +1471,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/mythic-keystone/period/index"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_mythic_keystone_period(
-        self,
-        period_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, period_id: int
     ) -> dict[str, Any]:
@@ -1705,5 +1481,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             period_id (int): The ID of the Mythic Keystone period to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1713,6 +1489,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/mythic-keystone/period/{period_id}"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_mythic_keystone_seasons_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
+        self, region: str, locale: str
     ) -> dict[str, Any]:
@@ -1722,4 +1499,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -1729,10 +1506,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/mythic-keystone/season/index"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_mythic_keystone_season(
-        self,
-        season_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, season_id: int
     ) -> dict[str, Any]:
@@ -1742,5 +1516,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             season_id (int): The ID of the Mythic Keystone season to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1750,3 +1524,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/mythic-keystone/season/{season_id}"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1755,7 +1530,3 @@ class WowGameDataApi(LocaleApi):
     def get_mythic_keystone_leaderboards_index(
-        self,
-        connected_realm_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, connected_realm_id: int
     ) -> dict[str, Any]:
@@ -1765,5 +1536,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             connected_realm_id (int): The ID of the connected realm.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1775,3 +1546,4 @@ class WowGameDataApi(LocaleApi):
         )
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1779,2 +1551,4 @@ class WowGameDataApi(LocaleApi):
         self,
+        region: str,
+        locale: str,
         connected_realm_id: int,
@@ -1782,5 +1556,2 @@ class WowGameDataApi(LocaleApi):
         period_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
     ) -> dict[str, Any]:
@@ -1790,2 +1561,4 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             connected_realm_id (int): The ID of the connected realm.
@@ -1793,4 +1566,2 @@ class WowGameDataApi(LocaleApi):
             period_id (int): The ID of the period to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1800,3 +1571,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/connected-realm/{connected_realm_id}/mythic-leaderboard/{dungeon_id}/period/{period_id}"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1805,8 +1577,3 @@ class WowGameDataApi(LocaleApi):
     def get_mythic_raid_leaderboard(
-        self,
-        raid: str,
-        faction: str,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, raid: str, faction: str
     ) -> dict[str, Any]:
@@ -1816,6 +1583,6 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             raid (str): The raid to retrieve the leaderboard for (e.g., "castle-nathria").
             faction (str): The faction to retrieve the leaderboard for (e.g., "alliance", "horde").
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1825,3 +1592,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/leaderboard/hall-of-fame/{raid}/{faction}"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1829,5 +1597,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_pets_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_pets_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -1836,4 +1602,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -1843,11 +1609,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/pet/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_pet(
-        self,
-        pet_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_pet(self, region: str, locale: str, pet_id: int) -> dict[str, Any]:
         """
@@ -1856,5 +1617,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pet_id (int): The ID of the battle pet to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1864,11 +1625,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/pet/{pet_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_pet_media(
-        self,
-        pet_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_pet_media(self, region: str, locale: str, pet_id: int) -> dict[str, Any]:
         """
@@ -1877,5 +1633,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pet_id (int): The ID of the battle pet to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1885,7 +1641,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/pet/{pet_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_pet_abilities_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_pet_abilities_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -1894,4 +1649,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -1901,10 +1656,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/pet-ability/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pet_ability(
-        self,
-        pet_ability_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pet_ability_id: int
     ) -> dict[str, Any]:
@@ -1914,5 +1666,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pet_ability_id (int): The ID of the pet ability to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1922,10 +1674,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/pet-ability/{pet_ability_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pet_ability_media(
-        self,
-        pet_ability_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pet_ability_id: int
     ) -> dict[str, Any]:
@@ -1935,5 +1684,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pet_ability_id (int): The ID of the pet ability to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -1943,3 +1692,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/pet-ability/{pet_ability_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1948,7 +1698,3 @@ class WowGameDataApi(LocaleApi):
     def get_playable_classes_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1958,4 +1704,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1966,11 +1712,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/playable-class/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_playable_class(
-        self,
-        class_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, class_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -1980,5 +1723,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             class_id (int): The ID of the playable class to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -1989,3 +1732,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/playable-class/{class_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -1993,6 +1738,5 @@ class WowGameDataApi(LocaleApi):
         self,
+        region: str,
+        locale: str,
         playable_class_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
         is_classic: bool = False,
@@ -2003,5 +1747,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             playable_class_id (int): The ID of the playable class to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -2012,10 +1756,8 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/playable-class/{playable_class_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pvp_talent_slots(
-        self,
-        class_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, class_id: int
     ) -> dict[str, Any]:
@@ -2025,5 +1767,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             class_id (int): The ID of the playable class to retrieve PvP talent slots for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2033,3 +1775,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/playable-class/{class_id}/pvp-talent-slots"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2038,6 +1781,3 @@ class WowGameDataApi(LocaleApi):
     def get_playable_races_index(
-        self,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -2047,4 +1787,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -2055,10 +1795,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/playable-race/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_playable_race(
-        self,
-        playable_race_id: int,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, playable_race_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -2068,5 +1806,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             playable_race_id (int): The ID of the playable race to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -2077,3 +1815,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/playable-race/{playable_race_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2082,3 +1822,3 @@ class WowGameDataApi(LocaleApi):
     def get_playable_specializations_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
+        self, region: str, locale: str
     ) -> dict[str, Any]:
@@ -2088,4 +1828,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -2095,10 +1835,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/playable-specialization/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_playable_specialization(
-        self,
-        spec_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, spec_id: int
     ) -> dict[str, Any]:
@@ -2108,5 +1845,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             spec_id (int): The ID of the playable specialization to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2116,10 +1853,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/playable-specialization/{spec_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_playable_specialization_media(
-        self,
-        spec_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, spec_id: int
     ) -> dict[str, Any]:
@@ -2129,5 +1863,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             spec_id (int): The ID of the playable specialization to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2137,3 +1871,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/playable-specialization/{spec_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2142,7 +1877,3 @@ class WowGameDataApi(LocaleApi):
     def get_power_types_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -2152,4 +1883,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -2160,11 +1891,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/power-type/index"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_power_type(
-        self,
-        power_type_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, power_type_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -2174,5 +1902,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             power_type_id (int): The ID of the power type to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -2183,3 +1911,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/power-type/{power_type_id}"
-        return self._get_static_resource(resource, region, locale, is_classic)
+        namespace = f"static-classic-{region}" if is_classic else f"static-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2187,5 +1917,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_professions_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_professions_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2194,4 +1922,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -2201,10 +1929,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/profession/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_profession(
-        self,
-        profession_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, profession_id: int
     ) -> dict[str, Any]:
@@ -2214,5 +1939,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             profession_id (int): The ID of the profession to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2222,10 +1947,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/profession/{profession_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_profession_media(
-        self,
-        profession_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, profession_id: int
     ) -> dict[str, Any]:
@@ -2235,5 +1957,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             profession_id (int): The ID of the profession to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2243,11 +1965,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/profession/{profession_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_profession_skill_tier(
-        self,
-        profession_id: int,
-        skill_tier_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, profession_id: int, skill_tier_id: int
     ) -> dict[str, Any]:
@@ -2257,6 +1975,6 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             profession_id (int): The ID of the profession.
             skill_tier_id (int): The ID of the skill tier to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2266,11 +1984,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/profession/{profession_id}/skill-tier/{skill_tier_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_recipe(
-        self,
-        recipe_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_recipe(self, region: str, locale: str, recipe_id: int) -> dict[str, Any]:
         """
@@ -2279,5 +1992,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             recipe_id (int): The ID of the recipe to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2287,10 +2000,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/recipe/{recipe_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_recipe_media(
-        self,
-        recipe_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, recipe_id: int
     ) -> dict[str, Any]:
@@ -2300,5 +2010,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             recipe_id (int): The ID of the recipe to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2308,3 +2018,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/recipe/{recipe_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2312,5 +2023,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_pvp_seasons_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_pvp_seasons_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2319,4 +2028,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -2326,10 +2035,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/pvp-season/index"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pvp_season(
-        self,
-        pvp_season_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pvp_season_id: int
     ) -> dict[str, Any]:
@@ -2339,5 +2045,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pvp_season_id (int): The ID of the PvP season to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2347,10 +2053,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/pvp-season/{pvp_season_id}"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pvp_leaderboards_index(
-        self,
-        pvp_season_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pvp_season_id: int
     ) -> dict[str, Any]:
@@ -2360,5 +2063,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pvp_season_id (int): The ID of the PvP season.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2368,11 +2071,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/pvp-season/{pvp_season_id}/pvp-leaderboard/index"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pvp_leaderboard(
-        self,
-        pvp_season_id: int,
-        pvp_bracket: str,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pvp_season_id: int, pvp_bracket: str
     ) -> dict[str, Any]:
@@ -2382,6 +2081,6 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pvp_season_id (int): The ID of the PvP season.
             pvp_bracket (str): The PvP bracket to retrieve (e.g., "2v2", "3v3").
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2391,10 +2090,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/pvp-season/{pvp_season_id}/pvp-leaderboard/{pvp_bracket}"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pvp_rewards_index(
-        self,
-        pvp_season_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pvp_season_id: int
     ) -> dict[str, Any]:
@@ -2404,5 +2100,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pvp_season_id (int): The ID of the PvP season.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2412,3 +2108,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/pvp-season/{pvp_season_id}/pvp-reward/index"
-        return self._get_dynamic_resource(resource, region, locale)
+        query_params = {"namespace": f"dynamic-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2417,7 +2114,3 @@ class WowGameDataApi(LocaleApi):
     def get_pvp_tier_media(
-        self,
-        pvp_tier_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pvp_tier_id: int
     ) -> dict[str, Any]:
@@ -2427,5 +2120,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             pvp_tier_id (int): The ID of the PvP tier to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2435,7 +2128,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/pvp-tier/{pvp_tier_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_pvp_tiers_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_pvp_tiers_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2444,4 +2136,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -2451,10 +2143,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/pvp-tier/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pvp_tier(
-        self,
-        pvp_tier_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pvp_tier_id: int
     ) -> dict[str, Any]:
@@ -2464,5 +2153,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pvp_tier_id (int): The ID of the PvP tier to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2472,3 +2161,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/pvp-tier/{pvp_tier_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2476,5 +2166,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_quests_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_quests_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2483,4 +2171,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -2490,11 +2178,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/quest/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_quest(
-        self,
-        quest_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_quest(self, region: str, locale: str, quest_id: int) -> dict[str, Any]:
         """
@@ -2503,5 +2186,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             quest_id (int): The ID of the quest to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2511,7 +2194,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/quest/{quest_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_quest_categories_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_quest_categories_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2520,4 +2202,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -2527,10 +2209,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/quest/category/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_quest_category(
-        self,
-        quest_category_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, quest_category_id: int
     ) -> dict[str, Any]:
@@ -2540,5 +2219,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             quest_category_id (int): The ID of the quest category to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2548,7 +2227,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/quest/category/{quest_category_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_quest_areas_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_quest_areas_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2557,4 +2235,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -2564,10 +2242,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/quest/area/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_quest_area(
-        self,
-        quest_area_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, quest_area_id: int
     ) -> dict[str, Any]:
@@ -2577,5 +2252,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             quest_area_id (int): The ID of the quest area to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2585,7 +2260,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/quest/area/{quest_area_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_quest_types_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_quest_types_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2594,4 +2268,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -2601,10 +2275,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/quest/type/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_quest_type(
-        self,
-        quest_type_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, quest_type_id: int
     ) -> dict[str, Any]:
@@ -2614,5 +2285,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             quest_type_id (int): The ID of the quest type to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2622,3 +2293,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/quest/type/{quest_type_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2627,7 +2299,3 @@ class WowGameDataApi(LocaleApi):
     def get_realms_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -2637,4 +2305,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -2645,11 +2313,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/realm/index"
-        return self._get_dynamic_resource(resource, region, locale, is_classic)
+        namespace = f"dynamic-classic-{region}" if is_classic else f"dynamic-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_realm(
-        self,
-        realm_slug: str,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, realm_slug: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -2659,5 +2324,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             realm_slug (str): The slug of the realm to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -2668,3 +2333,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/realm/{realm_slug}"
-        return self._get_dynamic_resource(resource, region, locale, is_classic)
+        namespace = f"dynamic-classic-{region}" if is_classic else f"dynamic-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2673,7 +2340,3 @@ class WowGameDataApi(LocaleApi):
     def get_regions_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -2683,5 +2346,5 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
+            is_classic (bool, optional): Whether to query for classic regions. Defaults to False.
 
@@ -2691,11 +2354,8 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/region/index"
-        return self._get_dynamic_resource(resource, region, locale, is_classic)
+        namespace = f"dynamic-classic-{region}" if is_classic else f"dynamic-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_region(
-        self,
-        region_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, region_id: int, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -2705,6 +2365,6 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             region_id (int): The ID of the region to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
-            is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
+            is_classic (bool, optional): Whether to query for classic regions. Defaults to False.
 
@@ -2714,3 +2374,5 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/region/{region_id}"
-        return self._get_dynamic_resource(resource, region, locale, is_classic)
+        namespace = f"dynamic-classic-{region}" if is_classic else f"dynamic-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2718,5 +2380,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_reputation_factions_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_reputation_factions_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2725,4 +2385,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -2732,10 +2392,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/reputation-faction/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_reputation_faction(
-        self,
-        reputation_faction_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, reputation_faction_id: int
     ) -> dict[str, Any]:
@@ -2745,5 +2402,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             reputation_faction_id (int): The ID of the reputation faction to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2753,7 +2410,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/reputation-faction/{reputation_faction_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_reputation_tiers_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_reputation_tiers_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2762,4 +2418,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -2769,10 +2425,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/reputation-tiers/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_reputation_tier(
-        self,
-        reputation_tiers_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, reputation_tiers_id: int
     ) -> dict[str, Any]:
@@ -2782,5 +2435,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             reputation_tiers_id (int): The ID of the reputation tier to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2790,3 +2443,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/reputation-tiers/{reputation_tiers_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2794,9 +2448,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_spell(
-        self,
-        spell_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_spell(self, region: str, locale: str, spell_id: int) -> dict[str, Any]:
         """
@@ -2805,5 +2453,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             spell_id (int): The ID of the spell to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2813,10 +2461,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/spell/{spell_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_spell_media(
-        self,
-        spell_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, spell_id: int
     ) -> dict[str, Any]:
@@ -2826,5 +2471,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             spell_id (int): The ID of the spell to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2834,3 +2479,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/spell/{spell_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2838,5 +2484,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_talents_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_talents_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2845,4 +2489,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -2852,11 +2496,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/talent/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_talent(
-        self,
-        talent_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_talent(self, region: str, locale: str, talent_id: int) -> dict[str, Any]:
         """
@@ -2865,5 +2504,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             talent_id (int): The ID of the talent to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2873,7 +2512,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/talent/{talent_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_pvp_talents_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_pvp_talents_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2882,4 +2520,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -2889,10 +2527,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/pvp-talent/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_pvp_talent(
-        self,
-        pvp_talent_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, pvp_talent_id: int
     ) -> dict[str, Any]:
@@ -2902,5 +2537,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             pvp_talent_id (int): The ID of the PvP talent to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2910,3 +2545,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/pvp-talent/{pvp_talent_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -2914,5 +2550,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_tech_talent_tree_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_tech_talent_tree_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2921,4 +2555,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -2928,10 +2562,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/tech-talent-tree/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_tech_talent_tree(
-        self,
-        tech_talent_tree_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, tech_talent_tree_id: int
     ) -> dict[str, Any]:
@@ -2941,5 +2572,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             tech_talent_tree_id (int): The ID of the tech talent tree to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2949,7 +2580,6 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/tech-talent-tree/{tech_talent_tree_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_tech_talent_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_tech_talent_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -2958,4 +2588,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
 
@@ -2965,10 +2595,7 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/tech-talent/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_tech_talent(
-        self,
-        tech_talent_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, tech_talent_id: int
     ) -> dict[str, Any]:
@@ -2978,5 +2605,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             tech_talent_id (int): The ID of the tech talent to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -2986,10 +2613,7 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/tech-talent/{tech_talent_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
     def get_tech_talent_media(
-        self,
-        tech_talent_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
+        self, region: str, locale: str, tech_talent_id: int
     ) -> dict[str, Any]:
@@ -2999,5 +2623,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             tech_talent_id (int): The ID of the tech talent to retrieve media for.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -3007,3 +2631,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/media/tech-talent/{tech_talent_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -3011,5 +2636,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_titles_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_titles_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -3018,4 +2641,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -3025,11 +2648,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/title/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_title(
-        self,
-        title_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_title(self, region: str, locale: str, title_id: int) -> dict[str, Any]:
         """
@@ -3038,5 +2656,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             title_id (int): The ID of the title to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -3046,3 +2664,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/title/{title_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -3050,5 +2669,3 @@ class WowGameDataApi(LocaleApi):
 
-    def get_toy_index(
-        self, *, region: OptionalRegion = None, locale: OptionalLocale = None
-    ) -> dict[str, Any]:
+    def get_toy_index(self, region: str, locale: str) -> dict[str, Any]:
         """
@@ -3057,4 +2674,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
 
@@ -3064,11 +2681,6 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/toy/index"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
-    def get_toy(
-        self,
-        toy_id: int,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-    ) -> dict[str, Any]:
+    def get_toy(self, region: str, locale: str, toy_id: int) -> dict[str, Any]:
         """
@@ -3077,5 +2689,5 @@ class WowGameDataApi(LocaleApi):
         Args:
+            region (str): The region to query.
+            locale (str): The locale to use for the response.
             toy_id (int): The ID of the toy to retrieve.
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
 
@@ -3085,3 +2697,4 @@ class WowGameDataApi(LocaleApi):
         resource = f"/data/wow/toy/{toy_id}"
-        return self._get_static_resource(resource, region, locale)
+        query_params = {"namespace": f"static-{region}", "locale": locale}
+        return super().get_resource(resource, region, query_params)
 
@@ -3090,7 +2703,3 @@ class WowGameDataApi(LocaleApi):
     def get_token_index(
-        self,
-        *,
-        region: OptionalRegion = None,
-        locale: OptionalLocale = None,
-        is_classic: bool = False,
+        self, region: str, locale: str, is_classic: bool = False
     ) -> dict[str, Any]:
@@ -3100,4 +2709,4 @@ class WowGameDataApi(LocaleApi):
         Args:
-            region (str, optional): the region to query (e.g., Region.US, "eu"). Defaults to None, in which case the default region provided at instantiation is used.
-            locale (str, optional): the locale to use for the response (e.g., Locale.es_MX, "en_GB"). Defaults to None, in which case the default locale provided at instantiation is used.
+            region (str): The region to query (e.g., "us", "eu").
+            locale (str): The locale to use for the response.
             is_classic (bool, optional): Whether to query for classic realms. Defaults to False.
@@ -3108,2 +2717,4 @@ class WowGameDataApi(LocaleApi):
         resource = "/data/wow/token/index"
-        return self._get_dynamic_resource(resource, region, locale, is_classic)
+        namespace = f"dynamic-classic-{region}" if is_classic else f"dynamic-{region}"
+        query_params = {"namespace": namespace, "locale": locale}
+        return super().get_resource(resource, region, query_params)
